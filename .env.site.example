# Site-Specific Configuration Template
# This file is for site-specific deployments and should NOT be committed to git
# Copy this to .env.site and configure for your specific deployment site

# Site Information
SITE_NAME=production-site-1
SITE_ENVIRONMENT=production
DEPLOYMENT_ID=prod-$(date +%Y%m%d)

# Site-Specific Database Configuration
# Configure for your specific PostgreSQL instance
POSTGRES_HOST=your-postgres-server.internal
POSTGRES_PORT=5432
POSTGRES_USER=mcp_prod_user
POSTGRES_PASSWORD=your_secure_production_password
POSTGRES_DB=mcp_production

# Site-Specific File Storage
# Configure for your site's file storage location
MCP_FILE_SERVER_BASE=/opt/site-data/mcp-files

# Site-Specific Julia Installation
JULIA_PATH=/opt/julia/bin/julia

# Site-Specific Security Settings
MCP_ALLOWED_HOSTS=your-server.domain.com,backup-server.domain.com
MCP_SSL_ENABLED=true
MCP_SSL_CERT_PATH=/etc/ssl/certs/mcp-server.crt
MCP_SSL_KEY_PATH=/etc/ssl/private/mcp-server.key

# Site-Specific Performance Settings
MCP_DB_MAX_CONNECTIONS=20
MCP_DB_CONNECTION_TIMEOUT=30
MCP_WORKER_PROCESSES=4

# Site-Specific Monitoring
MONITORING_ENABLED=true
METRICS_ENDPOINT=https://your-metrics-server.com/api/metrics
LOG_AGGREGATION_ENDPOINT=https://your-logs-server.com/api/logs

# Site-Specific Backup Configuration
BACKUP_ENABLED=true
BACKUP_SCHEDULE=0 2 * * *
BACKUP_RETENTION_DAYS=30
BACKUP_STORAGE_PATH=/opt/backups/mcp-server