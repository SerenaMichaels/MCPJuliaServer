# Local Development Environment Configuration
# This file is for site-specific settings that should NOT be committed to git
# Copy this to .env.local and configure for your local environment

# Local PostgreSQL Configuration
# These settings override the default .env values for local development
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_local_dev_password_here
POSTGRES_DB=postgres

# Local File Server Configuration
MCP_FILE_SERVER_BASE=/home/seren/mcp-data
# Or use a different local path:
# MCP_FILE_SERVER_BASE=/tmp/mcp-test-data

# Local Julia Configuration
JULIA_PATH=/home/seren/test-project/julia-1.11.2/bin/julia
# Or system Julia:
# JULIA_PATH=/usr/local/bin/julia

# Development Settings (only for local testing)
MCP_DEV_MODE=true
MCP_DEBUG_COMMUNICATION=true
MCP_CLIENT_LOG_LEVEL=DEBUG

# Local Database Connection Pool Settings
MCP_DB_MAX_CONNECTIONS=5
MCP_DB_CONNECTION_TIMEOUT=10

# Local Testing Database
# Use a separate database for testing to avoid conflicts
TEST_POSTGRES_DB=mcp_test
TEST_POSTGRES_USER=mcp_test_user
TEST_POSTGRES_PASSWORD=test_password_123